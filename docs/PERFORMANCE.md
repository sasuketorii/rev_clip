# 性能・検証メモ

## 未公開の品質修正候補

2026年9月16日の同条件比較・確認範囲・未達項目は[品質検証報告](QUALITY_REPORT.md)に集約しています。以下のv0.1.6以前の結果を、新しい候補の性能・実機確認の証拠として流用しません。

## v0.1.6: 現在の検証結果（2026年9月16日）

| 検証 | 結果 | 証拠の範囲 |
| --- | --- | --- |
| macOS XCTest | 225件成功（24.8秒） | ローカルテストの実行結果 |
| CLI・インストーラテスト合計 | 77件成功（5.9秒） | Python互換40件＋ネイティブ37件。37件は77件の内数 |
| ネイティブUniversal Debugコンパイル | arm64・x86_64とも成功 | 両アーキテクチャでの実行・性能測定とは別 |
| バグ報告Workerテスト | 23件成功、skip 0件 | ローカルテストの実行結果 |
| Finderでコピーしたファイルの画像表示 | 報告者が実機で確認済み | 報告された操作での確認。全ファイル形式の保証ではない |

テスト所要時間はアプリの起動時間ではありません。UIのAX（アクセシビリティ）テキストエリアは最終テストで到達性を確認済みです。ローカル検証と公開配布物の検証は区別します。配布版は[Releases](https://github.com/sasuketorii/rev_clip/releases)を参照してください。ローカル結果を、公開CI・署名／公証済み配布物の検証や性能対応全体の完了とは扱いません。

### Demo版の起動・終了・アイドル測定

測定ファイル `revclip-perf-v016-demo.json` の `v016-demo` を確認しました。ウォーム状態の1回測定で、対象は `revclip-demo`、観測は0〜9秒です。

| 項目 | 結果 |
| --- | --- |
| 起動からCLI応答可能になるまで | 512.035 ms（約512 ms） |
| 終了 | 226.647 ms（約227 ms） |
| RSS | 初回178.172 MiB、1〜9秒は178.438〜178.469 MiB（約178.5 MiBで安定） |
| CPU表示 | 初回70.8%、1秒時点1.0%、2〜9秒は0.0% |
| 累積CPU時間 | 初回0.38秒、5〜9秒は0.39秒 |

RSSは記録のKiBを1,024で割った値です。CPU 0.0%はサンプル表示の丸め値であり、CPU処理が一切ないことを意味しません。CLI応答可能になる時点はUI全体の準備完了とは異なります。比較基準なしの単一ローカル測定であり、コールド起動、通常版、別の履歴量・画像・テンプレート、他機種への一般化や改善倍率の算出はできません。

### SVG描画・キャッシュ取得

測定ファイル `revclip-svg-perf.json` に記録された8種類すべてで `rendered = 1` を確認しました。`cold_ms` は初回描画、`cache_hit_ms_mean_100` はキャッシュ取得100回の平均です。

| SVG | 初回描画（ms） | キャッシュ取得100回の平均（ms） |
| --- | ---: | ---: |
| openai | 5.786 | 0.00511 |
| claude | 2.597 | 0.00499 |
| gemini | 1.672 | 0.00501 |
| cursor | 1.790 | 0.00556 |
| grok | 1.869 | 0.00516 |
| kimi | 1.907 | 0.00530 |
| deepseek | 3.105 | 0.00536 |
| hermes | 19.835 | 0.00663 |

初回描画は1.672〜19.835 ms、キャッシュ取得の平均は0.00499〜0.00663 msでした。表の数値は丸めています。この記録には機種・OS・ビルド設定や分散・パーセンタイルの情報が含まれないため、それらを推定しません。単一のローカル測定であり、任意のSVG、メニュー表示全体、ホバー待ち時間を含む体感速度を保証しません。

### 今回の測定で確認していない範囲

通常版の起動性能、コールド起動、長時間稼働、他データ条件、アプリ全体のピークメモリ使用量は今回のDemo短時間測定では確認していません。以下の旧版測定をv0.1.6の比較基準にはしません。競合製品との比較値もありません。

## 過去の測定: 0.0.30の起動時CPU負荷修正

0.0.29では、起動時の履歴メニュー構築が画像を含む保存本文を先読みしていました。さらに`RCStorageCipher`の`Data(NSData)`がSwiftの`Sequence<UInt8>` initializerを選択し、全体を1バイトずつ取得・コピーしていました。暗号化判定と復号準備でこの変換を2回実行していました。

0.0.30では次のように変更しています。

- `NSData`から`Data`へは`as Data`で変換し、入力サイズは変換前に検査する。
- 暗号化形式の判定は先頭5バイトだけを読む。AES-GCMの認証、鍵、保存形式、保存後の検証は維持する。
- メニューの構築だけでは本文・サムネイルを読み込まない。本文はテキスト／URL／リッチテキスト項目のハイライト時、サムネイルは開いたメニューの直下の項目に限って読み込む。
- サムネイルは直列のutilityキューで処理し、キャッシュ済み画像を共有する。本文もutilityキューで処理し、読み込み中にメニューが再構築されても新しい項目は完了を待って更新する。

### 当時の検証

macOS 26.6.2、Apple M2 Max、Xcode 26.6、最適化有効の同一条件で、`RCStorageCipher`の実コードに合成データ8MiBを渡し、一時ファイルへの暗号化保存と復号読み込みを比較しました。テスト用の一時鍵を使い、復元内容が入力と一致することを確認しています。

| 処理 | 0.0.29 | 0.0.30 |
| --- | ---: | ---: |
| 保存（暗号化・復号による検証を含む） | 4.952秒 | 0.012秒 |
| 読み込み（判定・復号） | 3.304秒 | 0.007秒 |
| 合計 | 8.256秒 | 0.019秒 |

単一のローカル測定であり、アプリ全体の改善倍率や他機種での時間を保証する数値ではありません。

`make -C src/Revclip test`で以下の回帰を検査します。

- 大きな暗号化データの往復と、バイト単位の列挙回数／判定時の読み出し量。実時間の閾値に依存しません。
- 履歴メニュー構築時の本文・サムネイルI/Oゼロ、ハイライトした項目だけの本文取得、画像本文の取得ゼロ、閉じたフォルダのサムネイル取得ゼロ。
- 同じ画像の重複生成防止、同じ本文を読み込み中にメニュー項目が置き換わった場合の完了反映。
- 既存の暗号認証失敗、ファイル境界、保存・移行、メニュー等のテスト。

遅い変換、起動時先読み、読み込み完了を待たない動作を意図的に戻すと、対応する3種類の回帰テストが失敗することも確認しました。再発調査では、CPU使用率だけでなくmacOSの`cpu_resource`診断と実行バイナリのUUID・呼び出し経路を照合してください。
