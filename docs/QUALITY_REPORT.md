# v0.2.3（build 61）更新通知の修正 — 2026-09-19

- 原因: `LSUIElement` の常駐アプリで Sparkle の gentle reminders が未実装でした。定期更新の案内が他アプリの背後に表示され、通知や更新ボタンもありませんでした。
- 修正: standard user driver delegate を接続し、前面表示を控える場合は通知センターと専用メニューバーボタンで案内します。通知拒否でもボタンを維持し、クリックで Sparkle の既存セッションを開きます。再起動前の通知も起動時の delegate 登録で処理します。
- 最適化Releaseの全体テスト427件成功。追加の起動時登録・前プロセス通知クリック試験を含む最終の更新関連25件も成功。CI・配信結果は公開時に追記します。
- Fable 5.1による原因監査と修正レビュー完了。Demoビルド・署名検証成功。検証証拠は `.local/update-reminder-audit/`。
- 別bundle IDの実Sparkle定期確認で、修正前の背後ウィンドウと修正後の更新ボタンを観測。通知拒否を模した状態で、ボタンのクリック→更新案内→リマインダー撤去→「後で通知」による終了をAXと画面記録で確認。実際の通知センターバナーとインストール完了はこのharnessの検証範囲外です。
- この時点では以下の v0.2.2 が公開済みです。v0.2.3の配布・インストール結果は別途確認します。

## v0.2.2（build 60）の公開・品質確認 — 2026-09-18

- [公開版 v0.2.2](https://github.com/sasuketorii/rev_clip/releases/tag/v0.2.2)、対象SHA `0c6d7b63d03887c9ab0c4e1a467f6dda4a7f92cc`。
- セットアップを「権限ステータス」「クリップボードメニュー」「FasterOCR」の3タブに変更し、初期表示を権限にしました。既存の権限controllerとショートカット保存・競合判定を再利用し、サイドバーの権限導線も維持します。
- Woodpecker #11（同一SHA・push・初回）、[macOS CI](https://github.com/sasuketorii/rev_clip/actions/runs/35353122876)、[Release](https://github.com/sasuketorii/rev_clip/actions/runs/35354332892)の全工程が成功。最適化XCTest 420件、ローカル関連回帰49件が成功。今回は公開後の自動配信チェックも成功しました。
- Demo実画面で3タブの順序・権限表示・保存済みキー・競合拒否・権限への切替後の警告解除を確認。Fable 5.1の差分レビューでブロッカーなし。権限の実変更は行っていません。
- 公開DMGのDeveloper ID署名（Team `LLKWV86ZBP`）・entitlements・公証チケット・Gatekeeperを検証し、アプリ内公開鍵によるSparkle Ed25519検証も成功。匿名配信と通常版／Demoの照合receiptは `.local/release-v0.2.2/public-delivery.json` です。
- DMG SHA-256: `de21d63e3fcef171946e52008e0aac5697678dca295ae5646378b0f839c2c83b`。
- 通常版／Demoはともに **0.2.2 (60)**。通常版は公開DMG、Demoはタグの製品ソースから導入し、元バンドルとの実行ファイルhash一致と署名を確認。旧通常版を退避し設定・履歴は維持。通常版を起動済みです。
- 証拠は `.local/release-v0.2.2/` に保存。Intel／macOS 14実機、権限を付け外しした復帰、実Sparkle更新インストールは未確認です。配信receipt単独で公証・暗号署名・UIを証明するものではなく、別ログと画面記録も保持します。

---

# v0.2.1（build 59）の公開・品質確認 — 2026-09-18

- 公開タグ: [`v0.2.1`](https://github.com/sasuketorii/rev_clip/releases/tag/v0.2.1)、対象SHA `43e357db86c3c860091b1f48a6d70488970d2f68`。
- Woodpecker #9（push・同一SHA・初回）と[macOS CI](https://github.com/sasuketorii/rev_clip/actions/runs/35340107009)が成功。最適化XCTest 419件、ローカルの設定・ショートカット回帰48件が成功しました。
- Fable 5.1で保存・所有slot・競合判定・入力捕捉・各設定画面をレビューし、警告残留と入力捕捉の境界条件を修正。再レビューでリリースを止める指摘なし。レビューは実機試験の代替ではありません。
- 最終Demoで、メニューへのOCRキー割当拒否と保存値維持、OCRへの切替時の警告解除、自分のOCRキー再設定、OCR／ショートカット画面の競合拒否とページ往復後の警告解除をGUI入力とAX読み取りで確認。キーボード画像とPanic末尾の黄色文字も確認しました。
- [Release run](https://github.com/sasuketorii/rev_clip/actions/runs/35344841395)は署名・公証・公開に成功しましたが、最後の配信チェックは `GITHUB_TOKEN has an unexpected form` で失敗。ジョブ全体を成功とは扱いません。GitHubの新しい可変長installation tokenに対する旧255文字上限を公開後に修正し、長い偽tokenのAPI限定・非出力を含む18テストで検証。タグや公開assetは変更せず、配信検証だけを別実行しました。
- 匿名の公開配信検証、および通常版／Demoを含む再照合が成功。Developer ID（Team `LLKWV86ZBP`）、Hardened Runtime、timestamp、entitlements、DMG公証チケット、Gatekeeperを検証。アプリ内公開鍵によるSparkle Ed25519署名検証も成功しました。
- DMG SHA-256: `bf0b6098b674c8d7db6a14c87b30d0b74c1a0b9b3b7bc05b0d5320920744207c`。
- このMacの通常版とDemoはともに **0.2.1 (59)**。通常版は公開DMG、Demoはタグと同じ製品ソースから導入し、それぞれ元バンドルとの実行ファイルhash一致と署名を検証。旧通常版を退避し、設定・履歴は維持しました。
- 証拠: `.local/release-v0.2.1/public-delivery.json`、`final-ui-acceptance.json`、画面記録、署名検証ログ、Fableレビュー、CIログ。配信receipt単独では公証・暗号署名・実機受入を証明しないため、別証拠も保持します。
- 制約: 他アプリの非排他ホットキー登録は網羅検出できません。GUI試験は合成キー入力で、すべての物理入力・VoiceOver・第三者hookの抑止を保証しません。Intel／macOS 14の実機と、Sparkleによる実際の更新インストールは未確認です。

---

# v0.2.0の公開・品質確認 — 2026-09-17

## 公開配布物の確認

- [v0.2.0](https://github.com/sasuketorii/rev_clip/releases/tag/v0.2.0)（build 58）、タグのコミットは `9a7c782ebef1f41e82f9b7f93d4a1f7c1c2ec195`。
- GitHub Actions CI `35232721477` は同一SHAのpush初回で成功。Debug／Releaseそれぞれ382テスト成功、Universalビルド成功。
- 公開ジョブ `35234237574` の初回は署名・公証・公開まで成功し、最後の配信確認だけ匿名GitHub APIの403で失敗。再実行は既公開buildと同値のため単調増加ガードで停止。ジョブ全体を成功とは扱いません。公開後は再ビルドではなく配信検証だけを実行します。
- 公開URLから匿名取得したDMGのApple署名、stapled公証、Gatekeeper、arm64/x86_64、Sparkle Ed25519署名を独立検証して成功。インストール済み通常版の更新公開鍵・feed URLとも一致。
- DMG SHA-256: `ae480d6e615f131560eec44c5809155ac27dbf22f9e6f7c22a6ba0ea2f2dc799`。
- 証拠: `.local/revocr/public-delivery-v0.2.0.json`、`.local/revocr/public-v0.2.0/signature-verification.json`。通常版アプリの置換はしていません。

## build 58の最終ローカル検証

- 全体XCTest：382件成功、失敗0件（233.949秒）。実行中のソース変更なし。`.local/revocr/final58-full.log`。
- CLI・インストーラ・公開配信ガード：88件成功、失敗0件（21.349秒）。実サービスへ送信せず、隔離したテスト入力・ソケットを使用。`.local/revocr/final58-cli-tests.log`。
- 通常版／Demo版の機能差チェック成功。差分セキュリティ検査は69ファイル、指摘0件。gitleaksは未実施。`.local/revocr/final58-security.txt`。
- 現行ソースの認識部だけを最適化コンパイルした合成12例／100認識：完全一致11/12、CER 2/218 = 0.917%、初回500.440ms、warm p50 199.883ms／p95 376.503ms。auto・言語補正OFF、1280×720、macOS 26.6.2。この値には選択・画面取得・コピー・通知を含まず、実画面精度やアプリ全体の性能合格を示しません。過去の異なる稼働条件との速度比較にも使いません。`.local/revocr/benchmark58.jsonl`。
- ログイン時起動、更新後再起動、macOS 14・Intel・外部画面の混在倍率・長時間負荷は実機未実施です。実施済みの起動・再オープンと区別します。


以下は途中候補の履歴です。現行の公開版はv0.2.0ですが、公開・公証を全環境での実機受入完了とは扱いません。

- build 55の全体XCTestは377件成功、失敗0件（232.873秒）。実行中のソース変更なし。記録は `.local/revocr/services-full.log`。
- Demo 0.2.0 (55)を開発署名で導入し、バンドルの署名・実行ファイルの一致を確認。通常版0.1.9 (42)は置き換えていません。
- 利用者がOCRの操作感改善、メニューを閉じて選択へ移る動作、右クリック入口の表示を確認。全ブラウザでのサービス経由の入力・取消・時間切れは未検証です。
- build 56の全体XCTestは381件中380件成功、1件タイムアウト。`RCPreferencesSidebarTests.testPaletteRefreshPreservesActiveEditorUntilNextAppearanceVisit` のメインキュー待機（2秒）が未完了でした。当初は同時に行ったDemo操作の干渉を疑いましたが、操作なしでも再現し、下記のとおりテスト側の原因を修正しました。記録は `.local/revocr/final56-full.log`。
- build 56をDemoへ導入。再オープン時の履歴・テンプレートメニューは実ウィンドウ画像で確認しましたが、終了状態からの起動ではメニューが出ないことを再現。この時点では初回起動の修正が必要でした。build 58で修正・実機確認しています。
- macOS 14・Intel・外部画面の混在倍率・長時間負荷の実機試験は未実施です。以下の初期ベンチマークを最新候補の性能値として使いません。

詳細は [faster OCR](REV_OCR.md)、再発防止の設計・確認手順は [開発ドキュメント](FASTER_OCR_DEVELOPMENT.md)を参照してください。

## build 58の修正と実機確認

- 初回起動では常駐アプリがアクティブにならないため、前面化を待たずに既存のメインメニュー入口へ渡すよう修正。build 57の協調的なアクティブ化だけでは直らないことも実機で確認しました。
- 設定画面テストのタイムアウトは、テストがSwiftUI管理ビューへ入力欄を追加したことによる警告と、XCTestのシンボル解析待ちが原因でした。テスト用入力欄をウィンドウのcontentViewへ移動し、待ち時間を延ばさずに修正。関連50件が成功しています。
- Demo 0.2.0 (58)の初回起動・再オープンで履歴とテンプレートを含むメニューを画像確認。`open -g` と `-suppressLaunchMenu YES` の組み合わせでは、非アクティブ・表示ウィンドウなしを確認。そのプロセスを再オープンするとメニューが表示されました。
- 証拠は `.local/revocr/demo58-manifest.json`、`demo58-cold-menu.png`、`demo58-reopen-menu.png`、`demo58-background.json`。通常版0.1.9 (42)は保持しています。
- 実際のログイン、Sparkle更新後の再起動、Services未起動時のOSイベントは未実施です。イベント判定の単体テストと区別します。

## 初回候補の記録

# faster OCR（旧称RevOCR）候補の品質確認 — 2026-09-17

初回Demoの実機操作で利用者から精度・アイコン誤認識・書式・操作感の問題が報告されました。**初回build 43の実機受入は不合格**でした。後続の修正と、現在の状態・未検証の一覧は[faster OCRの記録](REV_OCR.md)を参照。以下の328件は初回候補の自動検証であり、この報告を否定するものではありません。

`feat/ocr`、開始HEAD `868553963898f8561ba19cde147fab1944a07962`上の未コミット変更で、0.2.0 (43)候補を準備。詳細と操作・制限は [faster OCRの記録](REV_OCR.md) を参照してください。以下の箇条書きは、初回の候補（build 43）の時点の記録です。以下の0.1.9記録は過去の公開版の結果で、OCRの受入証拠ではありません。

- 実装：AppKit／ScreenCaptureKit／Vision。一般クリップボードへの遅い書込・二重保存・消去後の復活を防ぐ受付と世代チェックを追加。取消はEsc・Delete（Backspace／前方削除）・右クリック・ショートカット再押下。
- 自動検証：中間の全体326件、重点40件、取消キー関連11件の成功後、最終製品ソースを固定して全体328件を再実行し、失敗0件（230.790秒）。差分セキュリティ検査PASS、独立コードレビューLGTM。実画面受入は未実施。
- 性能・精度：最適化ハーネスの合成12例／100認識で初回308.415ms、warm p50 124.135ms／p95 146.991ms。完全一致7/12、CER3.211%。通常Releaseの操作全体・長時間負荷・実画面精度の合格ではありません。
- 導入：Demoのみ0.2.0 (43)へ更新し旧バンドルを退避。候補は未起動・実機受入待ち。通常版0.1.9 (42)は保持。新候補の公開・公証・更新配信は未実施です。

# 既存機能の品質検証 — 2026-09-16 追補

## v0.1.9公開・更新配信の確認

[v0.1.9 (42)](https://github.com/sasuketorii/rev_clip/releases/tag/v0.1.9)を公開しました。製品SHAは`ae00ec899deb0c75003427f7f85597f0dd2715f8`です。[main CI](https://github.com/sasuketorii/rev_clip/actions/runs/35066025079)と[Releaseジョブ](https://github.com/sasuketorii/rev_clip/actions/runs/35067117216)は、このSHAのpushで初回成功しています。

- 公開CI: Debug 310件・失敗0件（236.940秒）、Release 310件・失敗0件（234.013秒）。ローカル308件で省略した2件のポーリング比較も実行。合成試験であり、実OSのコピー・貼り付けの受入とは区別します。
- 認証なしで公開DMGとlatestフィードを取得。タグSHA、latest版、フィードの0.1.9／42・URL・サイズを照合。DMG SHA-256は`5852b8f6b50c0f15e8290c035c6c6fa03dbf7dd9f8488038b94021c09f5c9c70`。
- Apple公証チケット、GatekeeperのNotarized Developer ID受理、ネスト署名、既存公開鍵によるSparkle Ed25519署名を検証。通常版アプリと2箇所の同梱CLIで既存の署名要件との一致、カバレッジ実行コードがないことを確認。
- 通常版を公開DMGのアプリへ差し替え、175ファイル・リンクが配布物と一致。起動後の読み取り専用CLI問い合わせに応答。旧ローカル候補は退避。Demoは同じ製品SHAの0.1.9 (42)で、175ファイル・リンクが検証済み署名候補と一致。通常版だけを起動しました。
- 履歴・設定・Keychainの初期化、履歴内容の読み取り、一般クリップボードを使う合成操作は実施していません。Finderプレビュー・連続コピー・実貼り付け・実際のSparkle更新操作の受入は別途必要です。

今回の表示不整合は、ローカル候補0.1.9の導入後も公開フィードが0.1.8だったことによります。再発防止として、公開先と両インストール先を照合する`verify_release_delivery.py`、11件の合成回帰、公開後のCIチェック、[完了判定手順](RELEASING.md#完了判定ローカル更新と公開配信を分ける)をmainへ追加しました。製品タグの後続コミット`ab016aa75ed9bace032cc693d4be71522db4ddf1`で、アプリの製品ソースは変えていません。[再発防止変更のCI](https://github.com/sasuketorii/rev_clip/actions/runs/35067147389)も初回成功。今回も同じ照合スクリプトを公開先と両アプリに対して実行し成功しています。単に候補をインストールしただけでは公開完了と報告しません。

公開・導入の証拠は`.local/quality/20260916-copy-latency/published/manifest.json`、`delivery.json`、CIログ、内包ファイル一覧に保存。以下の候補記録は公開前の履歴であり、現在の配信状態はこの節を参照してください。

## v0.1.8配布後：コピーの表示遅延の診断（修正前の記録）

対象ソースは`4c918a17dbc4444dea2ff84a6424dc2f8de7fed2`（製品コードはv0.1.8の`b57676b3b9173f82a1e10761552a1bff1364d480`と同一）。「コピーがすぐ反映されない」という報告について、取得と表示を分けて診断しました。

- **ソース上の事実**：監視は500ms間隔、タイマー許容遅延50ms。メインスレッドへの配送・コピー元のデータ供給・保存時間も加わるため、500msを応答時間の上限とは扱えません。
- **再現した表示上の問題**：保存完了通知が届いても、`RCMenuManager.rebuildMenu`は開いているメニューがある間、更新を保留します。最後のメニューが閉じた後に再構築します。メニュー表示前に最新の取得・保存を完了させる経路はありません。そのためコピー直後に開いた古い一覧は、保存が完了しても開き直すまで更新されません。
- **診断試験**：`RCMenuFreshnessTests`の3件で、保存済み行を表す合成データと製品の通知・開閉ガードを使用。複数通知とmain queueの進行後にも古い行と選択IDを保持し、最後のメニューを閉じると最新行が出ることを確認。閉じた状態では通知時に更新されます。描画・DB・実際のポップアップは代替しているため、この試験はコピー取得から実画面までの実機試験ではありません。PASSは現象の再現であり、不具合解消ではありません。
- **取得と保存の分離**：既存の`RCClipboardFlowTests`3件もRelease構成で成功。合成文字列100件の取得処理p50 0.106ms / p95 0.184ms / 最大0.592ms。保存境界を250ms遅延させても次の取得は0.467ms。これは監視待ち・暗号化保存・描画を含まない数値です。
- **別の遅延候補**：画像のサムネイル生成はDB挿入と保存完了通知より前で、保存キュー上にあります。画像処理が遅い場合は表示と後続保存を遅らせますが、今回の利用者の操作で発生したかは未確認です。
- **実プロセス**：通常版PID 9256を2秒採取した範囲ではmainは通常のイベント待機で、前回の終了待ち循環は確認されませんでした。症状発生時の採取ではないため、断続的な停止を否定する証拠ではありません。

証拠は`.local/quality/20260916-copy-latency/`のログ・receipt・sample。Releaseの対象6件は1.246秒で成功し、実行中のソース変更はありません。一般クリップボードや利用者の履歴を閲覧・変更していません。製品実装・監視既定値・インストール済みアプリは今回変更していません。

原因の範囲：**「保存できたのに開いている一覧へ反映しない」経路は確認済み**です。「閉じて数秒待っても反映しない」「コピーそのものが抜ける」現象まで同じ原因と断定していません。修正時は表示開始前の取得・保存との同期を検討し、開いた後の行移動による誤選択を防ぐ契約を維持する必要があります。

## 0.1.9 (42) ユーザーテスト候補：コピー直後のメニュー表示の修正

候補起動の確認では、ローカルビルドにカバレッジ計測が混ざり、CLI終了時に`default.profraw`が生成される問題も検出しました。Release・Demo構成の`ENABLE_CODE_COVERAGE`・`CLANG_ENABLE_CODE_COVERAGE`・`CLANG_COVERAGE_MAPPING`を明示的に無効にし、SwiftとObjective-Cの両方から計測用コードを除外します。これは暗号化・検証・プレビューを省略する高速化ではありません。合成回帰の計測値と、計測なしの候補に対する実機受入は区別します。

上の診断を受け、`4c918a17dbc4444dea2ff84a6424dc2f8de7fed2`を基準に修正しました。Fable 5.1による実装に対し、クリック経路・取消・生存期間・プレビュー処理を追加レビューしています。候補の対象SHA、通常版とDemo版の署名・バイナリハッシュ、インストール状態は`.local/quality/20260916-copy-latency/candidate-manifest.json`に記録します。この候補を公開Release・公証済み配布物・実機受入済みとは扱いません。

- **根本原因の確定**：ホットキー経路はDBから即座に一覧を組み立てて表示し、取得・保存との同期を持たなかった。ステータスバーのクリック経路はAppKitがmouse-downで`statusItem.menu`を同期的に開くため、表示前に取得を挟む経路がそもそも存在しない。どちらも開いている間は再構築しないため、直前のコピーは開き直すまで見えない。「監視500msそのもの」ではなく「表示と取得・保存が同期していない」ことが原因で、監視間隔は変更していない。
- **再現（修正前にRED）**：新規`RCMenuOpenPreflightTests`（隔離した名前付きペーストボード・専用DB・RAM鍵で、取得→暗号化保存→DB→通知→開閉ガードまで製品コードを実行。差し替えるのはポップアップの追跡セッション、ステータスアイテム（同じmenu配線を持つ代替物）、前面アプリ参照、強調表示の有無、ファビコン取得、掃除タイマーだけ）。ホットキー6件は修正前の製品で4件失敗、修正後は全件成功。クリック経路・生存期間の8件は、フックと世代ガードを外した対照ビルドで8件失敗、修正後は全件成功。
- **最小の修正（ホットキー）**：`RCClipboardService`に`observePendingClipboardChangeWithCompletion:`を追加。タイマーと同じ非強制ポーリングを監視キューで実行し、それ以前の取得・保存の後ろで完了を返す（停止中・取得拒否でも必ず完了。呼び出し側を待たせない）。`RCMenuManager`のホットキー2経路は、この完了後にDBから一覧を組み立てて表示する。完了はメインキューではなく`CFRunLoopPerformBlock`で配送し、ポップアップの追跡ループが従来どおりメインキュー処理（ホバープレビュー・ファビコン）を継続できる状態を保つ。メインからメインへ`dispatch_sync`する監視キューを待たない。
- **最小の修正（ステータスバーのクリック）**：ネイティブの開き方（mouse-down、押下ドラッグ選択、右クリック、アクセシビリティ、アンカー、形状）は一切変えない。開いた直後の`menuWillOpen:`で同じポーリング＋保存の事前処理を開始し、完了時（保存完了通知の後ろにメインキューで配送）に「同じ開いたままのセッション」で「保存が届いており」「どのメニューにも強調表示が無く」「サブメニューを追跡していない」場合だけ、一覧をその場で組み直す。いずれかの条件が崩れていれば従来どおり閉じるまで保留し、選択中の行を動かさず、メニューを閉じたり開き直したりしない。ホットキー時点・開いた時点で捕捉した貼り付け先アプリはそのまま維持する。
- **その場の組み直しの安全**：組み直しは項目の差し替えだけを行う（`populateStatusMenuContents`）。ネイティブの`statusItem.menu`の再接続と外観設定は通常の再構築側に分離し、追跡中のメニューには触れない。差し替えた見えている行には`menuWillOpen:`と同じ開いた時の処理（行スタイル、ファビコン、サムネイル読込）を共通ヘルパーで施す。テストで「再接続0回」「見えている行2件がサムネイル読込対象」を固定。
- **ホットキー待機中のキャンセル**：大きな画像の保存で事前処理が遅い間にEscape・入力・クリックがあった場合は、遅れてメニューを出さない（取得は残す）。判定はCoreGraphicsのセッション全体のイベント経過時間（`CGEventSourceSecondsSinceLastEventType`のkey-down／mouse-down）で、要求後に入力があったかを比較する。権限不要で、イベント監視やタップを追加しない。ホットキー自身のkey-downは要求より前なので対象外。押しっぱなしの自動リピートが保存より先に来た場合は取り消し扱いになる（次の押下で即表示）。
- **生存期間の無効化**：監視の停止は終了ドレイン・履歴消去・緊急消去でしか起きない。`RCClipboardService`が開始・停止のたびに進める`monitoringGeneration`を公開し、要求時に記録した値と完了時の値が異なる（停止、または停止→再開のABA）、監視停止中、履歴消去進行中のいずれかなら、ホットキーは表示せず、クリック経路は組み直さない。連打は最後の要求だけ、追跡中のメニューがある間の要求は捨て、前面アプリが変わった・終了した場合も表示しない（取得自体は捨てない）。
- **維持した契約（テストで固定）**：変更のない`changeCount`では再取得・上書き・並べ替えを発生させない（観測1回、通知1回、`update_time`不変）。内部書込世代の除外、取得と保存の分離、順序・ドレイン、暗号化、プレビュー、外観、500ms既定、v0.1.8の終了修正は不変。診断3件は変更なしで成功。
- **実測（Release・RC_TESTING・合成テキスト・同一Mac）**：ホットキー要求からポップアップ到達まで、修正前（古い一覧のまま）は履歴6.1ms／ステータス2.3ms、修正後（最新行あり）は履歴4.2〜15.8ms、ステータス4.2〜7.5ms。ステータスバークリックは、開いてからその場の組み直しまで6.1〜12.1ms。変更のない世代では5.6〜9.7ms。修正前に最新行が見えるまでの時間は、次のポーリング待ち（タイマー間隔と許容遅延に加え、メイン配送・供給側のデータ提供・保存・描画の時間が乗るため上限は示せない）と閉じて開き直す操作を要していたため、同じ尺度では比較できない。取得のみはp50 0.106ms／p95 0.207ms／最大0.817ms（100件）。
- **回帰**：1回目の追補ではRelease全件がテストホスト1回目で301件中300件成功・1件クラッシュ、2回目で301件成功。クラッシュは`RCPreferencesSidebarTests`のテキスト編集中に入力メソッド`IMKClient`が`addSuiteNamed:`を呼び、テスト隔離用の`RCMemoryTestDefaults`が意図どおりabortしたもの。2回目の追補で、テスト専用の隔離層だけを「`com.apple.`接頭辞のシステムスイートは無視、それ以外は従来どおりabort」に狭め（全アクセサはメモリから応答するので実設定へは到達しない）、`RCTestDefaultsIsolationTests`で固定。再実行が通ったことを解決とは扱っていない。最終のRelease全件（長時間ポーリング計測`RCClipboardPollingTests`2件は変更対象外のため明示的にスキップ。同日の先行2回では2件とも成功）は308件成功・失敗0・ホスト再起動なし。
- **限界**：クリック経路の「開いた時点の初回描画」は従来どおり組み立て済みの一覧で、最新行はその後（テキストで数ms、大きな画像では暗号化・サムネイル完了後）に組み直しで現れる。開いてすぐ行を強調表示した、あるいはフォルダを開いた場合はその場では更新せず、閉じて開き直すまで待つ。AppKitが開いたままのメニューを実際に再描画する見え方（ちらつき・高さ変化）は合成テストでは代替できず、候補インストール後の利用者確認が必要。実機（ホットキー連打・ホバープレビュー・実貼り付け）も未確認。

## v0.1.8緊急修正：終了待ちで監視も停止する根本原因

v0.1.7を利用者の通常環境へインストール後、「終了できずコピーもできない」と報告されました。起動プロセスの存在だけでは操作受入にならないことが実際に露呈しました。以下は同日の追補です。

- 実プロセスの3秒sampleで、メインキューのメニューアクション → `terminate:` → `_shouldTerminate` → modal run loopの待機を確認。保存キューでの処理中ではありませんでした。
- 原因：`NSTerminateLater`を返す前に監視を止め、保存完了返信と5秒タイムアウトをmain dispatchへ配送していました。外側のmain dispatchブロックがAppKitの終了待ちから戻れず、その内側で同じキューの返信を実行できない循環待ちです。[Appleの終了待ち仕様](https://developer.apple.com/documentation/appkit/nsapplication/terminatereply/terminatelater?language=objc)では`NSModalPanelRunLoopMode`が使われます。
- 修正：返信はcommonとmodalを明示したmain run loopへ非同期配送。タイムアウトはutilityキューで発火させ、同じrun loopの返信経路を使います。保存ドレイン、5秒での取消・監視再開、世代チェック、二重返信防止を維持。
- 検出漏れ：旧テストはアプリを代替オブジェクトにし、外側のmain dispatchブロックを保持するAppKitの入れ子ループを再現していませんでした。新規2テストはこの状態で背景ドレインと実5秒タイムアウトを検証。修正前に両方失敗、修正後に成功しました。
- 実AppKitの隔離プロセスでも、製品の終了メソッドをそのまま使い、修正前は`DRAIN_COMPLETE`後に8秒watchdog、修正後は`APPKIT_TERMINATION_CONFIRMED`。ドレインを保留した別試行では5秒後の取消と`monitoring=1`を確認。クリップボード・DBは代替実装、ウィンドウなしで、利用者の履歴や一般クリップボードを読みません。
- 関連Release試験31件成功、全体Release試験290件成功（50.580秒）。既定監視と54条件の追加比較は前回から変更しておらず、今回のローカル全体試験では長時間の追加比較のみ除外。配布CIでは通常の全体検証を実行します。
- Finderプレビュー修正はv0.1.6から含まれ、画像読込実装はv0.1.7でも同一。関連7件は成功していますが、今回の表示報告が終了待ち停止の結果か、別条件での不具合かは未確認です。

今回の証拠は`.local/quality/20260916-termination/manifest.json`と同ディレクトリのlog・receiptです。[mainのCI](https://github.com/sasuketorii/rev_clip/actions/runs/35057122868)と[Releaseワークフロー](https://github.com/sasuketorii/rev_clip/actions/runs/35057124900)は同じ対象SHAで成功しました。修正SHAは`b57676b3b9173f82a1e10761552a1bff1364d480`、公開タグは[v0.1.8](https://github.com/sasuketorii/rev_clip/releases/tag/v0.1.8)。通常版・Demoを0.1.8 / build 41へ更新しました。通常版は公証済み公開DMGから差し替え、起動後に読み取り専用の設定問い合わせへの応答を確認しました。停止中の旧通常版プロセスは、同じ終了待ちと保存キュー非実行を再採取した後にSIGTERMで終了。データの初期化・履歴の閲覧は行っていません。

公開DMG SHA-256: `5a958f0c2119a167f7125a4c03fe558e546c845f186f2e8c15d35ef762db03f7`。公証チケット、Gatekeeper、内包署名、既存公開鍵によるSparkle署名、公開フィードのURL・サイズ・版を確認しました。署名済みバンドルと各インストール先の全ファイルも一致しています。利用者によるFinderプレビュー・コピー・終了の確認は別途必要です。下記はv0.1.7時点の記録で、同版を不具合なしと保証するものではありません。

## v0.1.7時点の結論と対象

**履歴利用・同時刻の再利用順を修正し、利用者の明示的な承認でv0.1.7を公開しました。製品全体の受入は未完了です。** 専用環境は現在用意できないため、利用者が直接確認する方針です。通常セッションで合成コピーや実挿入試験を代替していません。通常版とDemoを0.1.7 (40)へ更新し、通常版は正常終了後に再起動しました。履歴・設定・Keychainの初期化はしていません。

- 今回の開始SHA: `a36c71ef9d1139340ce098333225c1a5dd033c61`。
- 公開版: **0.1.7 (40)**。タグ対象SHA: `b7744954bce34730dd3bffc649c23bfc3bc48655`。検証済みソース `38f4fee30c32b267e4dd2b2ba1ebde8803930378` と同一ツリーです。ローカル候補とCI配布物の署名済みバイナリは区別し、`.local/quality/20260916-followup/candidate-manifest.json` に記録します。
- 環境: Apple M2 Max / 96 GB / macOS 26.6.2 / Xcode 26.6。比較はRelease arm64の隔離XCTestホストで実施。通常Releaseの起動・実挿入とは別です。
- 新機能、UI再設計、既定値・監視間隔・権限要件の変更なし。初回の時間窓撤廃、取得／保存分離、貼り付け期限、終了ドレイン、データ同一性を維持。

## 1. 履歴利用の経路と修正

修正前のソースでは `RCMenuManager.selectClipMenuItem:` → `pasteClipWithDataHash:targetApplication:` → `RCPasteService.pasteClipData:toApplication:` の復元後に内部changeCountが登録され、取得側はその世代をスキップしていました。履歴利用を別途更新する呼び出しはなく、更新処理は外部再コピーの `handleExistingClipWithHash:...` に残っていました。

隔離DBと認証付き暗号化原本を使い、**実際のメニュー選択 → 原本読み込み → 復元 → DB照会**で再現しました。ペーストボード書き込みとキー送信は合成境界です。実OSへの挿入成功とは扱いません。

修正後の経路:

```text
RCMenuManager: 選んだ履歴の保存済みhashを渡す
  → RCPasteService: 復元成功時のみ recordHistoryUseWithDataHash:
  → RCClipboardService: 取得キュー → 保存キュー
  → RCDatabaseManager: 既存行だけUPDATE
  → DBに確定した更新日時で通知
```

テンプレートには履歴hashを渡しません。旧形式の保存済みhashも、復元した内容から推定し直さず選択した行の識別子を維持します。内部changeCount除外は維持し、監視を時間窓で止めません。

| overwrite | reorder | 外部から同じ内容を再コピー | 履歴選択・復元成功 |
| --- | --- | --- | --- |
| OFF | OFF | 更新しない | 更新しない |
| ON | OFF | 最新へ | 更新しない |
| OFF | ON | 更新しない | 最新へ |
| ON | ON | 最新へ | 最新へ |

更新の境界は**クリップボードへの復元成功**です。自動ペーストOFFでも利用に数え、後のキー送信キャンセルは復元成功を取り消しません。失敗した書き込みは更新しません。送信完了時に二重更新しません。これは設定名・既存コメントの「再コピー」と「再利用」の区別に合わせた修正で、キー配送の成功通知を新設したものではありません。

受付済みの更新は直後のstop／終了でもドレインします。stop後の新規受付は拒否し、Clearはドレイン後に削除します。削除された行へ遅延UPDATEしてもINSERTはしません。クリップ取得と同じキュー順を使います。`RCHistoryUseTests` で設定4通り×自動ペーストON/OFF、送信成功mock、取消、失敗、テンプレート、旧hash、停止・消去・通知時刻を確認しました。

## 2. 同一ミリ秒と時計の巻き戻り

基準側でAを挿入→Bを挿入→Bと同時刻でAを再利用すると、Bが先頭になり、件数1への整理でAが消えることを再現しました。時計の巻き戻りでも利用順が逆転しました。

`RCDatabaseManager` のINSERT／UPDATE一文内で、`max(要求したepochミリ秒, 保持中の最大update_time + 1)` を保存します。SELECTと書き込みの間に他の接続を挟まず、再起動後もDBの最大値から続けます。DB形式・保存ファイル・既存行を一括変更しません。従来の同時刻行は `id DESC` を決定的な補助順として維持します。64bit上限なら安全に書き込み失敗とし、既存行を変更しません。

`RCHistoryRetentionTests` でA/B/A、時計巻き戻り、DB再オープン、後続挿入、表示順、保持対象、上限時の非変更を確認しました。通知する時刻も保存後の行から読み直します（既存のスクリーンショット保存経路を含む）。

**制約:** 更新日時を後退させないため、大きい時計巻き戻りでは期限削除が実時間より遅れる場合があります。件数整理は最後の更新順を守ります。期限用と順序用の別スキーマ導入は今回行いません。この制約は両READMEにも記載しています。

## 3. 再現・回帰の実行結果

| 実行 | 結果 | 証拠 |
| --- | --- | --- |
| 基準a36の製品コード＋最小再現テスト | 14件実行、6メソッドで15アサーション失敗。復元並び替え、再コピー設定、同一ms、巻き戻り等を再現 | `revclip-followup-red.log` / receipt / `baseline-tests/` |
| 関連Release | 40件成功、9.116秒 | `revclip-followup-focused.log` / receipt / `focused.xcresult` |
| Release全体（長い比較実験は別実行） | 288件成功、44.641秒 | `revclip-followup-release.log` / receipt / `release.xcresult` |
| Debug全体（同上） | 288件成功、43.124秒 | `revclip-followup-debug.log` / receipt / `debug.xcresult` |
| Release監視比較 | 54試行、実験テスト成功、166.477秒 | `revclip-followup-polling.log` / receipt / `polling.xcresult` |

各harness receiptで実行中のソース変更なしを確認。新しい候補の全体試験には、前回の暗号化・内部世代・処理順・貼り付け先・終了・消去回帰も含みます。初回の271件成功だけを今回の根拠として使いません。差分の独立レビューでは具体的なP0/P1指摘なしですが、実機の未知問題が存在しないという意味ではありません。

## 4. 500ms監視の同条件比較

製品コードの監視間隔は500msを維持しました。**試験専用のサブクラス**で既存タイマーの間隔を変更し、取得処理は現行コードを通します。3案とも名前付きOSペーストボード、同じ合成文字列、コピー6回、開始位相73/137/311ms、終了待ち600msで比較しました。保存境界はメモリ上の記録で、暗号化ディスク保存の性能試験ではありません。

- 固定500ms。
- 変化を取得した後だけ250msへ短縮。
- 変化を取得した後だけ100msへ短縮。
- 短縮案は最後の観測から1秒、連続最大2秒。その後は最低1秒500msへ戻す。コピー生成側からのヒント・新規権限なし。
- 3秒アイドルも各案3回。実行順を位相ごとに回転。CPU/RSSはXCTestホスト全体（計測処理込み）。

### 捕捉数（各セル18回生成、3位相合算）

| コピー間隔 | 固定500ms | 短時間250ms案 | 短時間100ms案 |
| --- | ---: | ---: | ---: |
| 1000ms | 18 | 18 | 18 |
| 500ms | 18 | 18 | 18 |
| 250ms | 11 | 16 | 17 |
| 100ms | 6 | 7 | 11 |
| 50ms | 4 | 4 | 4 |

全案合計270生成 = 188観測・処理 + **82観測前の上書き**。停止時未観測0、**取得後の処理欠落0**。処理済みはディスク永続化済みという意味ではありません。

### 呼び出しと負荷

| 案 | コピー試行の監視回数 | 同CPU時間 / 経過時間 | 3秒×3アイドルの監視回数 / CPU時間 |
| --- | ---: | --- | --- |
| 固定500ms | 82 | 263.087ms / 46.295s | 18 / 30.974ms |
| 短時間250ms | 131 | 277.836ms / 46.503s | 18 / 32.265ms |
| 短時間100ms | 287 | 297.556ms / 46.464s | 17 / 30.794ms |

RSSのサンプルピークは全案約73.9MiB。同じプロセスのウォーム状態・計測自体を含むため、案ごとのメモリ差や常駐消費電力を証明しません。各試行の取得待ちP50/P95、実時刻、CPU/RSSは `polling-comparison.json` と `polling-comparison.md` に保存しています。取得待ちはメニュー表示や原本読み込みを含む貼り付け時間ではありません。

**判断:** 250/100ms間隔では改善が見られる一方、50msでは改善なし。50ms試行の監視回数だけが4→8→18へ増えています。初回観測より前に終わるコピー列は、観測後の短縮では回収できません。今回の短い合成結果だけで短縮案を既定採用しません。製品へ適用するなら、通常Releaseでの常駐・混合負荷確認と既定動作変更の承認を別途必要とします。常時極短間隔への変更や権限追加はしていません。

## 5. 配布候補と未実施の実機受入

通常ReleaseはDeveloper ID、DemoはApple Development署名で生成し、両方のarm64/x86_64、Hardened Runtime、ネストした署名、テスト用鍵注入シンボル・テストバンドルの不在を確認。108資源中107は同一で、残りのCLIは署名が異なりますが両アーキテクチャのコード部分は一致しました。ID・保存先・更新フィードの差は意図どおりです。

候補DMGのチェックサム、読み取り専用マウント後の署名、内包アプリ171ファイルと元ビルドの一致を確認し、アンマウントしました。DMG SHA-256は `c3c8b983356b2f9434584a645396ed2e600828a4c1bb61dcd3979619c4cf2e85`。これはローカル候補の検証記録です。続いて公開されたDMGの検証結果は以下です。

- [Releaseワークフロー](https://github.com/sasuketorii/rev_clip/actions/runs/35055371778)成功。タグSHAから署名・公証・更新フィードを生成。
- [公開v0.1.7](https://github.com/sasuketorii/rev_clip/releases/tag/v0.1.7)から認証なしでDMGとlatestフィードを取得。公開DMG SHA-256: `f4e9c52c75b3f4d52c8663e24d7d04f08e79d53f222b7087f6c2453bcd6e76d5`。
- stapler検証、ネストした署名、GatekeeperのNotarized Developer ID受理、既存アプリの公開鍵によるSparkle Ed25519署名、フィードの版・サイズ・URLを確認。
- 通常版は公開DMGから、Demoは同じ製品ソースの署名済み候補から更新。各インストール先の全ファイルが元バンドルと一致。旧アプリはローカル証拠内へ退避。
- 通常版の再起動後プロセスを確認。Demoはインストールのみ。Sparkle経由の実更新、利用者による操作受入は未確認。

| 実機項目 | 今回の状態 |
| --- | --- |
| 通常Releaseの実挿入・対象切替・連続コピー | 未実施：専用セッションなし |
| 更新に伴う通常版の終了・再起動 | 正常終了と更新後のプロセス起動を確認。保存内容の復元試験は未実施 |
| メニュー開始→表示、項目選択→原本読込→対象への挿入のP50/P95 | 未測定 |
| 10分アイドルCPU/RSS、30分混合操作 | 未測定 |
| Intel実機・最低対応OS | 未確認 |

再開に必要な本人操作は、専用Mac／VM、またはmacOSの「システム設定 → ユーザとグループ」でテスト用標準ユーザーを用意し、そのユーザーでログインすることです。そのセッションで作業環境を開き、必要なアクセシビリティ等のOS許可を本人が与えてから、合成データ・空の検証文書だけで通常Releaseを検証します。普段のセッションのクリップボード退避や権限変更で代替しません。候補SHAとmanifestを引き継ぎ、開始前に再確認します。

## 6. 証拠・再実行

今回の証拠は `.local/quality/20260916-followup/`（Git対象外）。初回の `.local/quality/20260916/` と混ぜません。比較用のテスト原本、log、receipt、xcresult、候補manifestを保存します。結果SHAとPR CIの結論もmanifestに記録します。実行指示Markdownは未追跡のままで、コミット・pushに含めません。

```sh
make -C src/Revclip setup
# 隔離Release全体。長い比較実験は次の呼び出しで単独実行する。
xcodebuild -project src/Revclip/Revclip.xcodeproj -scheme Revclip \
  -configuration Release -destination 'platform=macOS' \
  -derivedDataPath .local/quality/recheck \
  ENABLE_TESTABILITY=YES SWIFT_ACTIVE_COMPILATION_CONDITIONS=RC_TESTING \
  'GCC_PREPROCESSOR_DEFINITIONS=$(inherited) RC_TESTING=1' \
  CODE_SIGNING_ALLOWED=NO -parallel-testing-enabled NO \
  -skip-testing:RevclipTests/RCClipboardPollingTests/testBoundedAdaptivePoliciesAgainstFixed500ms test
# 上のskip-testingを以下へ差し替えると比較実験だけを実行できる:
# -only-testing:RevclipTests/RCClipboardPollingTests/testBoundedAdaptivePoliciesAgainstFixed500ms
python3 scripts/tests/polling_comparison.py PATH_TO_EXPERIMENT_LOG
python3 scripts/check_demo_parity.py
```

以下は初回修正時の測定を残した記録です。今回のSHA・候補の実機受入や新たな性能改善の証拠へ流用しません。

---

# 初回修正の検証記録（a36c71e、追補前）

## 初回記録の結論

**条件付きの候補です。公開Release・タグ・更新配信は実施していません。**
取得・保存・貼り付け待機・終了・履歴の同一性を修正しました。新機能やUI再設計はありません。隔離したRelease XCTestでは全271件が成功しましたが、通常Releaseによる実際の入力先への挿入、長時間の常駐性能、配布後の更新までを合格にしたものではありません。

## 2. 基準と対象

- 開始コミット: `878b42b45f21e1495d3c10c211406a8fe41abfbf`（公開v0.1.6 / build 39）。
- 候補: 未公開の **0.1.7 / build 40**。修正ブランチ: `quality/current-flow-20260916`。候補の確定SHA・バイナリSHA-256はローカル証拠の `candidate-manifest.json` に記録します。
- 環境: Apple M2 Max、96 GB、macOS 26.6.2 (25G83)、Xcode 26.6 (17F113)、macOS SDK 26.5。
- 比較: Release / arm64の同一テストフラグ、名前付きペーストボード、メモリ上の設定、隔離DB、使い捨て暗号鍵。通常起動のReleaseやDemoの測定と混同しません。
- 基準側には試験環境の隔離と再現テストだけを追加。製品の取得・保存処理は変更していません。

## 3. 直した問題

| 問題と根拠 | 修正 | 回帰確認 |
| --- | --- | --- |
| 内部貼り付けの約1秒間、後続の外部コピーB/Cまで取得されない。基準側の合成試験で再現 | 時間窓のフラグを廃止し、内部書き込み後のchangeCountだけを除外 | B/Cを順序どおり取得・処理 |
| 前の保存処理が次の取得を待たせる。250ms遅延注入で再現 | 取得と保存の直列キューを分離 | 次の取得待ちを比較、飽和時の順序検証 |
| 貼り付け待機中のクリップボード変更・対象変更の検査不足（ソースと合成状態遷移） | 操作世代・対象・前面・changeCountを再検証。取得できたAX入力欄が変わった場合もキャンセル | 別アプリ、終了、固定期限後の遅延コールバック、AX中の期限超過、次の操作、書き込み失敗 |
| 終了時に保存キューの完了を待たない（ソース） | 新規取得を止めて両キューをドレイン。5秒で完了しなければ終了を取り消す | 遅延完了、重複終了、タイムアウト、監視再開 |
| 同時刻の履歴の表示順と保持対象の不一致（合成DB） | 表示も `update_time DESC, id DESC` に統一 | 件数削除・再オープン後の一致 |
| 異なるデータ形式が同じハッシュ入力になる（合成構造） | フィールド名・有無・長さ・配列境界を含むv2同一性。旧ハッシュ一致は認証済み原本の全フィールド比較後にだけ再利用 | 旧行・旧ファイル保持、形式差の別保存、RTF/RTFD往復 |

旧ハッシュの再現は構造的な合成ケースであり、一般アプリが同じ不正RTFDを生成することを確認したものではありません。保存形式・暗号方式の移行や既存データの一括書き換えは行いません。

## 4. 正しさの結果

- Release XCTest: **271件成功、40.278秒**。実行中のソース変更なしをharness receiptで確認。
- Debug XCTest: **271件成功、39.275秒**。Releaseとは別実行。
- CLI／スキルインストーラ: **77件成功、6.030秒**。合成Unix socketと一時ホームのみ。その内数であるネイティブ37件を、署名済み候補の同梱CLIでも再実行し成功（11.209秒）。
- 10,000件の取得済み合成オブジェクトを250件ずつ処理し、順序・同一オブジェクト・待機カウンタ復帰を確認。10,000回のOSコピーやディスク保存の試験ではありません。
- 実際の隔離暗号化ファイル保存をINSERT前で止め、ドレイン→履歴消去後に行・ファイル・遅延通知が復活せず、テンプレートが残ることを確認。
- キューの目安は保存中を含む16件／100MiB。満杯なら取得済み原本を保持して待機します。設定上許可された大きい1件は単独で処理します。取得済み待機1件、暗号処理、画像の作業領域は別に存在するため、アプリの総メモリ100MiB上限ではありません。
- 全体試験の初回では、試験用オブジェクトの終了処理漏れと、ホスト言語を参照する期待値の不一致が発生。試験の隔離を修正した後に全体を再実行しました。初回失敗を成功として数えていません。

## 5. 同条件の性能比較

| 条件 | 基準 | 修正後 | 解釈 |
| --- | ---: | ---: | --- |
| 保存境界に250ms遅延を注入した次の取得待ち（各1回） | 261.374ms | 4.710ms | 直前の保存待ちによる遅延を分離。体感全体の倍率ではない |
| 軽い取得100回 P50 | 0.084ms | 0.092ms | 取得単体は高速化したとは言えない |
| 同 P95 | 0.189ms | 0.193ms | 同上 |
| 同 最大 | 0.456ms | 0.405ms | P99は報告しない |

実際の500ms監視タイマーと名前付きOSペーストボードで、各間隔6回ずつ書き込み、最後に600ms待ちました。保存境界はメモリ上の観測です。

| コピー間隔 | 生成数 | 基準: 観測／処理 | 修正後: 観測／処理 |
| --- | ---: | ---: | ---: |
| 1000ms | 6 | 6 / 6 | 6 / 6 |
| 500ms | 6 | 6 / 6 | 6 / 6 |
| 250ms | 6 | 3 / 3 | 3 / 3 |
| 100ms | 6 | 2 / 2 | 2 / 2 |
| 50ms | 6 | 1 / 1 | 1 / 1 |

観測前に上書きされたコピーは回収できません。少数試行・タイマーとの位相に依存する結果で、一般的な捕捉率ではありません。監視間隔の既定値は維持しました。メニュー体感P95、実際の挿入、通常Releaseの10分アイドルCPU／RSS、30分混合操作は**未測定**です。短時間のテスト所要時間から推定しません。

## 6. 維持したもの

承認済みUI、既定値、テンプレート編集、プレビュー、原本、暗号化、CLIの機能範囲を維持しました。通常版・Demo版は同じソースで、機能分岐の静的検査に成功しています。バイナリのID・保存先・更新フィードの差は意図されたものです。インストール済みの両アプリは更新・終了・再起動していません。

## 7. 安全性と限界

取得前の除外アプリ／秘匿型チェック、暗号認証、履歴非公開CLI、削除境界の既存回帰を含めています。ユーザーの一般クリップボード・設定・保存データ・Keychain項目の書き換えは行いません。外部へのバグ報告、スキルの実ホームへの配置、ネットワークプレビューの実送信は今回の試験対象ではありません。

AX入力欄を初回から取得できないアプリでは、従来の貼り付け機能を維持し、アプリ単位で検査します。同じ入力欄の維持は保証できません。既存のCGEvent配送経路を維持しており、検査とOSによる配送は原子的ではありません。550msは操作開始からの送信可否の期限であり、同期OS呼び出しを中断して戻す実行時間保証ではありません。暗号化のOS権限に対する限界は[SECURITY.md](../SECURITY.md)を参照してください。

## 8. 配布候補

通常ReleaseとDemoを、テストフラグを含まない別のビルド領域で生成しました。

| 対象 | 確認済み | 未確認 |
| --- | --- | --- |
| 通常Release 0.1.7 (40) | arm64/x86_64、Developer ID署名、Hardened Runtime、ネストしたCLI／frameworkの厳密署名検証 | 通常起動・実挿入・長時間性能 |
| Demo 0.1.7 (40) | arm64/x86_64、Apple Development署名、厳密署名検証、独立した保存先、更新フィードなし | 通常起動・実挿入 |
| 同梱物 | 両版ともテスト用鍵注入シンボル・テストバンドルなし。108資源中107が同一。残りのCLIは署名が異なるが、両アーキテクチャのコードセクションは同一 | 全OS環境での挙動同一性 |
| 候補DMG | 既存デザインから生成、チェックサム検証、読み取り専用マウント、内包アプリ171ファイルが元候補と一致、署名再検証、アンマウント | 公証・Gatekeeper受理・インストール・Sparkle更新 |

SHA・バイナリSHA-256・成果物相対パスは `candidate-manifest.json` に記録しています。公開Release、更新配信、実環境へのインストール／更新は未実施です。公開v0.1.6の署名・公証成功をこの変更へ流用しません。

## 9. 残る確認と公開条件

専用Mac／VM／ログインユーザーで、通常Releaseの入力先への挿入、入力欄変更、連続操作、終了・再起動、メニュー応答、10分アイドル・30分混合操作を確認する必要があります。この作業中は専用セッションを確保できていません。普段使いの一般クリップボードを使うことで代替しません。

以上の実機確認なしに公開更新としての合格は出していません。公開許可はユーザーからありますが、品質条件が未完了であるため公開せず候補を保持します。監視間隔の変更やAX未対応アプリの自動貼り付け方針を変える場合は、性能・互換性を測定したうえで製品判断を分離します。

## 10. 証拠と再確認

ローカル証拠: `.local/quality/20260916/`（Git対象外）。比較ログ、receipt、`polling-comparison.json`、候補manifestを保存します。`Release.xcresult` と `Debug.xcresult` も保存しています。XCTestの元の位置は実行ログに残っています。

```sh
make -C src/Revclip setup
xcodebuild -project src/Revclip/Revclip.xcodeproj -scheme Revclip \
  -configuration Release -destination 'platform=macOS' \
  -derivedDataPath .local/quality/recheck \
  ENABLE_TESTABILITY=YES SWIFT_ACTIVE_COMPILATION_CONDITIONS=RC_TESTING \
  'GCC_PREPROCESSOR_DEFINITIONS=$(inherited) RC_TESTING=1' \
  CODE_SIGNING_ALLOWED=NO -parallel-testing-enabled NO test
python3 scripts/check_demo_parity.py
```

このコマンドはテスト専用ホストです。通常版・Demoの起動コマンドではありません。実行指示の原本Markdownはコミット・pushの対象外です。
