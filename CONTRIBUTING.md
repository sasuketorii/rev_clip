# Contributing

バグ報告、UIの改善、翻訳、PRを歓迎します。大きな動作変更は、先にIssueで使い方と目的を共有してください。

1. Fork・clone後、`make -C src/Revclip setup` でプロジェクトを生成します。
2. 変更は `src/Revclip/project.yml` とソースへ加えます。生成された `.xcodeproj`、ビルド成果物、個人設定はコミットしません。
3. `make -C src/Revclip test` を実行します。UIやペーストの変更は、実アプリでマウスとキーボードの両方を確認してください。
4. PRには「変わる動作」「確認方法」「未確認の環境」を書いてください。クリップボードの実データや認証情報をログ・スクリーンショットに含めないでください。

macOS 14を動作対象として維持しています。依存の追加は、OS標準機能や既存ライブラリで扱えない理由とともに提案してください。

提供するコードは、このプロジェクトのMITライセンスで配布できるものに限ります。
